# Миграция на SQLite

## ✅ Что было сделано:

1. **Изменен провайдер БД** с PostgreSQL на SQLite в `prisma/schema.prisma`
2. **Удалена зависимость** `pg` (PostgreSQL драйвер) из `package.json`
3. **Обновлен `.env.example`** с SQLite URL
4. **Добавлены файлы БД** в `.gitignore`

## 📝 Что нужно сделать:

### 1. Обновите `.env` файл:

Измените `DATABASE_URL` на:
```env
DATABASE_URL="file:./dev.db"
```

### 2. Переустановите зависимости:

```bash
npm install
```

Это удалит ненужный пакет `pg`.

### 3. Пересоздайте базу данных:

```bash
# Удалите старую БД (если была)
rm -f dev.db dev.db-journal

# Сгенерируйте Prisma Client
npm run db:generate

# Создайте новую SQLite базу данных
npm run db:push
```

### 4. Перезапустите сервер:

```bash
npm run dev
```

## ✅ Готово!

Теперь все данные будут храниться в файле `dev.db` в корне проекта.

## Преимущества SQLite:

- ✅ Не нужен отдельный сервер БД
- ✅ Все данные в одном файле
- ✅ Легко бэкапить (просто скопировать файл)
- ✅ Работает на любом хостинге
- ✅ Идеально для продакшена

## Бэкап:

Для бэкапа просто скопируйте файл:
```bash
cp dev.db dev.db.backup
```

## Для продакшена:

Используйте тот же файл или создайте отдельный:
```env
DATABASE_URL="file:./production.db"
```
