# 🔧 Инициализация базы данных

## Проблема: Пользователь не найден

Если вы видите ошибку "Bu tg_username (super_admin) bilan foydalanuvchi topilmadi", это означает, что база данных пустая и нужно создать пользователей.

## ✅ Решение:

### Вариант 1: Через браузер (самый простой)

1. Убедитесь, что сервер запущен: `npm run dev`
2. Откройте браузер и перейдите на: `http://localhost:3000/api/init`
3. Или используйте любой HTTP клиент (Postman, Insomnia) и сделайте POST запрос на `http://localhost:3000/api/init`

### Вариант 2: Через скрипт

```bash
cd backend-nextjs
node init-db.js
```

### Вариант 3: Через PowerShell

```powershell
$body = @{}
$response = Invoke-RestMethod -Uri "http://localhost:3000/api/init" -Method Post -Body ($body | ConvertTo-Json) -ContentType "application/json"
Write-Host $response
```

## После инициализации:

Будут созданы:
- ✅ SUPER_ADMIN пользователь
  - Username: `super_admin`
  - Password: `super_admin123`
- ✅ Default DOCTOR пользователь
  - Name: `Doktor`
  - Phone: `+998900000001`

## Теперь можно войти:

Используйте эти учетные данные для входа через `/api/auth/login`:
- `tg_username`: `super_admin`
- `password`: `super_admin123`

## Автоматическая инициализация:

Middleware пытается автоматически инициализировать БД при первом запросе, но это может не сработать надежно. Поэтому лучше вызвать `/api/init` вручную один раз после создания БД.
