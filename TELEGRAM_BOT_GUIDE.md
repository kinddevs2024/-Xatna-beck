# 🤖 Telegram Bot - Полное руководство

## ✅ Что реализовано:

1. **Полная функциональность бронирований через бота**
2. **Интерактивные кнопки (inline keyboard)**
3. **Просмотр доступных слотов**
4. **Отмена бронирований**
5. **Автоматическая регистрация пользователей**

## 🚀 Как использовать:

### 1. Настройка

1. Убедитесь, что в `.env` есть токен:
   ```env
   BOT_TOKEN=your_bot_token_here
   ```

2. Перезапустите сервер:
   ```bash
   npm run dev
   ```

3. Проверьте статус бота:
   ```bash
   curl http://localhost:3000/api/telegram/status
   ```

### 2. Команды бота:

- `/start` - Начать работу с ботом, регистрация
- `/book` - Создать новое бронирование
- `/mybookings` - Просмотреть свои бронирования
- `/available` - Просмотреть доступные временные слоты
- `/cancel` - Отменить бронирование
- `/help` - Справка по командам

### 3. Как работает создание бронирования:

1. Пользователь отправляет `/book` или нажимает кнопку "📅 Yangi bron qilish"
2. Бот показывает календарь с доступными датами (30 дней вперед)
3. Пользователь выбирает дату
4. Бот показывает доступные времена для выбранной даты
5. Пользователь выбирает время
6. Бот показывает подтверждение с деталями бронирования
7. Пользователь подтверждает
8. Бронирование создается в базе данных

### 4. Интерактивные кнопки:

Бот использует inline keyboard для удобной навигации:
- Кнопки выбора даты
- Кнопки выбора времени
- Кнопки подтверждения/отмены
- Быстрые действия (мои бронирования, помощь и т.д.)

## 🔧 Технические детали:

### Инициализация:

Бот инициализируется автоматически при старте сервера через:
1. `middleware.ts` - импортирует `telegram-init.ts`
2. `telegram-init.ts` - импортирует `telegram.service.ts`
3. `telegram.service.ts` - конструктор создает бота с polling

### Состояния пользователей:

Бот хранит состояния пользователей в `Map<userId, state>` для обработки многошаговых процессов (создание бронирования).

### Проверка доступности слотов:

Бот использует `BookingService.checkTimeSlotAvailability()` для проверки доступности временных слотов.

## 🐛 Отладка:

### Бот не отвечает:

1. Проверьте токен в `.env`
2. Проверьте логи сервера при запуске:
   ```
   ✅ Telegram Bot initialized successfully with polling
   ✅ Telegram Bot handlers setup completed
   ```
3. Проверьте статус: `GET /api/telegram/status`

### Ошибки при создании бронирования:

1. Убедитесь, что в базе есть доктор с `working: true`
2. Проверьте, что выбранное время не занято
3. Проверьте логи сервера на ошибки

### Polling не работает:

1. Проверьте интернет-соединение
2. Проверьте, что токен валидный
3. Перезапустите сервер

## 📱 Пример использования:

```
Пользователь: /start
Бот: 👋 Salom! [кнопки: 📅 Yangi bron, 📋 Mening bronlarim, ...]

Пользователь: [нажимает "📅 Yangi bron qilish"]
Бот: 📅 Yangi bron qilish. Avval sanani tanlang: [календарь]

Пользователь: [выбирает дату]
Бот: ⏰ Vaqtni tanlang. Sana: 2026-01-27 [времена: 09:00, 10:00, ...]

Пользователь: [выбирает время]
Бот: ✅ Bron ma'lumotlari... Bronni tasdiqlaysizmi? [✅ Tasdiqlash / ❌ Bekor]

Пользователь: [нажимает "✅ Tasdiqlash"]
Бот: ✅ Bron muvaffaqiyatli yaratildi! Bron #123...
```

## 🎯 Дополнительные возможности:

- Автоматические уведомления при изменении статуса бронирования
- Broadcast рассылки от админов
- Просмотр истории бронирований
- Отмена активных бронирований

## ⚠️ Важно:

- Бот работает только с пользователями, у которых есть `tg_id` в базе данных
- Бронирования создаются с фиксированной длительностью 30 минут
- Рабочие часы доктора: 9:00 - 18:00
- Бот автоматически регистрирует новых пользователей при `/start`
