export default function Home() {
  return (
    <div style={{ padding: '2rem', fontFamily: 'system-ui' }}>
      <h1>Doctor Appointment API</h1>
      <p>Backend API для системы записи к доктору</p>
      <p>API endpoints доступны по адресу: <code>/api/*</code></p>
    </div>
  );
}
