# Быстрое подключение фронтенда к бэкенду (локально)

## ✅ Что уже настроено:

1. **Фронтенд** - файл `.env` создан с настройками для `http://localhost:3000`
2. **Бэкенд** - CORS настроен для работы с localhost
3. **API конфигурация** - обновлена для использования локальных URL

## 🚀 Запуск:

### 1. Запустите бэкенд:

```bash
cd backend-nextjs
npm run dev
```

Бэкенд будет на: **http://localhost:3000**

### 2. Запустите фронтенд:

```bash
cd Xatna_Front
npm run dev
```

Фронтенд будет на: **http://localhost:5173** (или другой порт)

## ✅ Проверка:

1. Откройте браузер: `http://localhost:5173`
2. Откройте консоль разработчика (F12)
3. Проверьте Network tab - запросы должны идти на `http://localhost:3000/api/*`

## 🔧 Если что-то не работает:

### Проблема: CORS ошибки

**Решение:** Убедитесь, что:
- Бэкенд запущен на `http://localhost:3000`
- В `backend-nextjs/.env` есть `NODE_ENV=development`
- Перезапустите бэкенд после изменения `.env`

### Проблема: Запросы идут на старый URL

**Решение:** 
- Перезапустите фронтенд (Vite нужно перезапустить после изменения `.env`)
- Очистите кеш браузера (Ctrl+Shift+R)

### Проблема: API не отвечает

**Решение:**
- Проверьте, что бэкенд запущен: откройте `http://localhost:3000/api/doctor` в браузере
- Проверьте консоль бэкенда на наличие ошибок
- Убедитесь, что база данных настроена: `npm run db:push`

## 📝 Тестовые запросы:

```bash
# Проверка доктора (публичный endpoint)
curl http://localhost:3000/api/doctor

# Вход (создайте SUPER_ADMIN через /api/init)
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"tg_username":"super_admin","password":"super_admin123"}'
```

## 🎯 Готово!

Теперь фронтенд подключен к бэкенду локально. Все API запросы будут идти на `http://localhost:3000/api/*`
