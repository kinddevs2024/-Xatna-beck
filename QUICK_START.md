# Быстрый старт

## 1. Установка

```bash
npm install
```

## 2. Настройка .env

Скопируйте `.env.example` в `.env` и заполните:
- `DATABASE_URL` - строка подключения к PostgreSQL
- `JWT_SECRET` - секретный ключ (минимум 32 символа)
- Остальные переменные из `.env.example`

## 3. Инициализация БД

```bash
npm run db:generate
npm run db:push
```

## 4. Запуск

```bash
npm run dev
```

Сервер запустится на `http://localhost:3000`

## 5. Инициализация пользователей

Отправьте POST запрос на `/api/init` или просто сделайте любой API запрос - пользователи создадутся автоматически.

## Готово! 🎉

API готов к работе. Все endpoints доступны по адресу `/api/*`

## Основные endpoints:

- `POST /api/bookings` - Создать бронирование (30 минут)
- `GET /api/bookings` - Получить все бронирования
- `PATCH /api/bookings/:id/status` - Изменить статус
- `POST /api/auth/login` - Вход
- `GET /api/doctor` - Получить информацию о докторе
