# Telegram Bot Setup

## ✅ Что было сделано:

1. **Установлена библиотека** `node-telegram-bot-api` для работы с Telegram Bot API
2. **Создан сервис** `lib/services/telegram.service.ts` для отправки сообщений
3. **Реализована отправка broadcast** в `/api/posts/broadcast`
4. **Добавлены уведомления** при создании и изменении статуса бронирований

## 📝 Настройка:

### 1. Получите токен бота:

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен
4. Добавьте токен в `.env` файл:
   ```env
   BOT_TOKEN=your_bot_token_here
   ```

### 2. Перезапустите сервер:

```bash
npm run dev
```

При запуске вы должны увидеть:
```
✅ Telegram Bot initialized successfully
```

Если видите:
```
⚠️ BOT_TOKEN not found in environment variables
```
Проверьте, что токен правильно указан в `.env` файле.

## 🔧 Функциональность:

### 1. Broadcast сообщения (`POST /api/posts/broadcast`)

Отправляет сообщение всем клиентам, у которых есть `tg_id`:

**Пример запроса:**
```json
{
  "title": "Важное объявление",
  "description": "Текст сообщения",
  "image_url": "https://example.com/image.jpg" // опционально
}
```

**Ответ:**
```json
{
  "message": "Post muvaffaqiyatli yuborildi",
  "totalClients": 10,
  "sentCount": 8,
  "failedCount": 2
}
```

### 2. Уведомления о бронированиях

Автоматически отправляются:
- При создании нового бронирования
- При изменении статуса бронирования

**Формат уведомления:**
```
📅 Yangi bron qilindi!

📆 Sana: 2026-01-27
⏰ Vaqt: 10:00
👨‍⚕️ Shifokor: Dr. Smith
📊 Holat: PENDING
```

## ⚠️ Важные замечания:

1. **Telegram ID клиентов**: Уведомления отправляются только клиентам, у которых в базе данных есть `tg_id`
2. **Блокировка бота**: Если клиент заблокировал бота, отправка не удастся, но это не вызовет ошибку
3. **Лимиты API**: Telegram имеет лимиты на отправку сообщений (30 сообщений в секунду). Код включает задержку 100ms между отправками

## 🐛 Отладка:

Если бот не работает:

1. Проверьте токен в `.env`:
   ```bash
   echo $BOT_TOKEN  # или проверьте файл .env
   ```

2. Проверьте логи сервера при запуске

3. Убедитесь, что клиенты имеют `tg_id` в базе данных:
   ```sql
   SELECT id, name, tg_id FROM users WHERE role = 'CLIENT';
   ```

4. Проверьте, что бот не заблокирован пользователем

## 📚 Дополнительная информация:

- [Telegram Bot API Documentation](https://core.telegram.org/bots/api)
- [node-telegram-bot-api GitHub](https://github.com/yagop/node-telegram-bot-api)
